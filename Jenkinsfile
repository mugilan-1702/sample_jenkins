pipeline {
    agent { label 'slave' } 
    stages {
       stage ("build") {
          steps { 
             echo "Build stage"
             
                }
              }
       stage ('Deploy') {
           steps {
             echo "Deploy stage" 
                 }
               }
             }
          }
